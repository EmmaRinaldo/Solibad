class BidController {
    async createBid(req, res) {
      // Logique pour créer une offre
    }
  
    async getBid(req, res) {
      // Logique pour récupérer une offre par ID
    }
  
    async updateBid(req, res) {
      // Logique pour mettre à jour une offre par ID
    }
  
    async deleteBid(req, res) {
      // Logique pour supprimer une offre par ID
    }
  }

  export default new BidController();